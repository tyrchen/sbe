name: release-cli

on:
  push:
    tags:
      - 'sbexec-v*'

permissions:
  contents: read

jobs:
  verify-tag:
    name: Verify release tag and manifest version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Verify exact tagged source and version
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          version=$(cargo metadata --locked --no-deps --format-version 1 \
            | jq -r '.packages[] | select(.name == "sbexec") | .version')
          test "$GITHUB_REF_NAME" = "sbexec-v$version" || {
            echo "::error::tag $GITHUB_REF_NAME does not match sbexec $version"
            exit 1
          }

  create-release:
    name: Create GitHub release
    needs: verify-tag
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - name: Generate changelog
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5 # v4
        id: git-cliff
        with:
          config: cliff.toml
          args: -vv --latest --strip header
        env:
          OUTPUT: CHANGES.md
      - name: Create or update release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body: ${{ steps.git-cliff.outputs.content }}
          draft: true

  build:
    name: Build ${{ matrix.target }}
    needs: create-release
    runs-on: ${{ matrix.runner }}
    environment: release
    permissions:
      contents: write
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux: static musl binaries, run on Ubuntu.
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
          - target: aarch64-unknown-linux-musl
            runner: ubuntu-latest
          # macOS: dynamic-linked darwin binary on Apple Silicon.
          - target: aarch64-apple-darwin
            runner: macos-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Install Rust
        run: rustup toolchain install stable --profile minimal
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          key: ${{ matrix.target }}

      # Build SBE from the exact tagged checkout and locked dependency graph
      # before the real release build. No older, unattested SBE binary enters
      # the release job. On native targets the sandboxed build warms the upload
      # action's cache; cross-musl targets use a host-native sandboxed check.
      - name: Build tagged sbe (dogfood)
        run: cargo install --path apps/cli --locked --force
      - name: Pre-build under sbe (dogfood)
        shell: bash
        env:
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          case "$TARGET" in
            aarch64-apple-darwin)
              rustup target add "$TARGET"
              sbe run --allow-insecure-linux-network -- \
                cargo build --release --locked --target "$TARGET" \
                --manifest-path apps/cli/Cargo.toml
              ;;
            *)
              # x86_64/aarch64 musl on ubuntu need a musl/cross linker that
              # upload-rust-binary-action provisions internally — avoid
              # duplicating that here. A host-native `cargo check` still
              # gives us a real sbe workload to dogfood against.
              sbe run --allow-insecure-linux-network -- cargo check --locked \
                --manifest-path apps/cli/Cargo.toml
              ;;
          esac

      - name: Generate SPDX SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          path: .
          format: spdx-json
          output-file: sbe-${{ matrix.target }}.spdx.json
          upload-artifact: false
          upload-release-assets: false

      - name: Build and upload binary to release
        id: upload
        uses: taiki-e/upload-rust-binary-action@f0d45ae91ee7b8ee928de7a9d04d893a08bcbec6 # v1
        with:
          bin: sbe
          target: ${{ matrix.target }}
          archive: $bin-$tag-$target
          tar: all
          zip: none
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: refs/tags/${{ github.ref_name }}
          manifest_path: apps/cli/Cargo.toml
          locked: true
          checksum: sha256
          include: LICENSE.md,README.md,sbe-${{ matrix.target }}.spdx.json

      - name: Attest release archive provenance
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3
        with:
          subject-path: ${{ steps.upload.outputs.tar }}

  publish-release:
    name: Publish complete release
    needs: build
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
    steps:
      - name: Publish draft after every target succeeds
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --draft=false --latest
