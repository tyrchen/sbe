name: build

on:
  push:
    branches:
      - master
    tags:
      - v*
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  build-rust:
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.platform }}
    # services:
    #   postgres:
    #     image: postgres:14.5
    #     env:
    #       POSTGRES_PASSWORD: postgres
    #     ports:
    #       - 5432:5432
    #     # Set health checks to wait until postgres has started
    #     options: >-
    #       --health-cmd pg_isready
    #       --health-interval 10s
    #       --health-timeout 5s
    #       --health-retries 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          submodules: recursive
      - name: Install Rust
        run: rustup toolchain install stable --component llvm-tools-preview
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@d630c3ba8c9bb0648bcefda869fb0d9b98c611d9 # cargo-llvm-cov
      - name: install nextest
        uses: taiki-e/install-action@b2876a75f58e1551c204bbfe1b56c63dabd6fcb0 # nextest
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
      # - name: Install Protoc
      #   uses: arduino/setup-protoc@a8b67ba40b37d35169e222f3bb352603327985b6 # v2.1.0

      # Dogfood the latest audited release. The setup action verifies the
      # archive checksum and GitHub build-provenance attestation before install.
      # nextest stays outside because its proxy tests bind random localhost TCP
      # ports.
      - name: Install sbe 0.4.2 (dogfood)
        uses: tyrchen/sbe@dba413595698f878a07c2106b45bf7b115ad1bea # sbexec-v0.4.2
        with:
          version: '0.4.2'
      - name: sbe smoke
        run: |
          sbe --version
          sbe profiles >/dev/null

      - name: Check code format
        run: cargo fmt --all -- --check
      - name: Check the package for errors
        run: sbe run -- cargo check --all
      - name: Lint rust sources
        run: sbe run -- cargo clippy --all-targets --all-features --tests --benches -- -D warnings
      - name: Execute rust tests
        run: cargo nextest run --all-features

  security:
    name: dependency and workflow security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust
        run: rustup toolchain install stable --profile minimal
      - name: Install security tools
        uses: taiki-e/install-action@d630c3ba8c9bb0648bcefda869fb0d9b98c611d9
        with:
          tool: cargo-audit@0.22.1,cargo-deny@0.19.8,cargo-machete@0.9.2
      - name: Check workflow security invariants
        run: ruby .github/scripts/check-workflow-security.rb
      - name: Scan RustSec advisories
        run: cargo audit
      - name: Check dependency policy
        run: cargo deny check advisories bans licenses sources
      - name: Check unused dependencies
        run: cargo machete
